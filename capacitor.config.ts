import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ikenna.mymovies',
  appName: 'myMovies',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
